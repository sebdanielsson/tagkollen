## Summary

<!-- What does this change and why? -->

## Checklist

- [ ] Builds and tests pass (`xcodebuild test`)
- [ ] `swiftformat` and `swiftlint` are clean
- [ ] Checked on both iPhone (compact) and iPad (regular) layouts, if UI changed
- [ ] New user-facing strings added to `Localizable.xcstrings` with a Swedish translation
